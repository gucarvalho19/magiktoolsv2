-- Migration 003 down: Placeholder (webhook_events removido)
-- Esta migration foi removida mas o Encore espera encontrá-la
-- Não faz nada para evitar quebrar o deploy

-- Nenhuma mudança no schema
SELECT 1;
